[project]
name = "lesserv-cloud"
version = "0.1.0"
description = "Control plane for a fleet of VLESS/Xray nodes"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
